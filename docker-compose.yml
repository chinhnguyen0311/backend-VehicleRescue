version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgis/postgis:15-3.4
    container_name: postgres_db_vehicle_rescue
    environment:
      POSTGRES_DB: vehiclerescue_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      TZ: Asia/Ho_Chi_Minh
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./dump-db_vehicle_rescue.sql:/docker-entrypoint-initdb.d/dump-db_vehicle_rescue.sql
    networks:
      - db_network
    restart: unless-stopped
volumes:
  postgres_data:

networks:
  db_network: